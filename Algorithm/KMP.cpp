#include<iostream>
#include<cstring>


const int N = 10010;
char s[N]; //长文本
strlen(s) = n;
char p[N]; //模式串(需要匹配的字符串)
strlen(p) = m;
int next[N];

//求next数组
for (int i = 2;i <= m;i++) {
    while (j && p[j + 1] != p[i])
}

